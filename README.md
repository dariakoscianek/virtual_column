# Virtual columns in pandas dataframe task
Function that adds a new column to a pandas DataFrame based on a specific operation.

## Supported operations
- Addition (+)
- Subtraction (-)
- Multiplication (*)

## Run tests
```bash
pip install -r requirements.txt
pytest